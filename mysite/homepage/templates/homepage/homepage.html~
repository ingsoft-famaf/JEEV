{% extends 'homepage/base.html' %}
{% block content %}
<body class="{% if request.user.is_authenticated %}logged-in{% else %}logged-out{% endif %}">

    {% if request.user.is_authenticated %}
        {%load static%}
        {% if request.user.first_name or request.user.last_name %}
            {{ request.user.first_name }} {{ request.user.last_name }}
        {% else %}
            {% if user.is_staff %}
                <h3>Hola administrador</h3>
<!--                <a href="/admin/" class="pull-right">Admin</a>-->
                <li><h4><a href="/superuser/">Interfaz de Administrador</a></h4></li>
                <p><a href="/accounts/logout/" class="pull-right">Logout</a>
                <p>    {{ request.user.username }} has loged in </p>
            {% else %}
                <h2>hola!</h2>
                <a href="/accounts/logout/" class="pull-right">Logout</a>
                <p>    {{ request.user.username }} has loged in </p>
            {% endif %}
        {% endif %}
    {% else %}
        <h3>No esta logueado, debe loguearse primero</h3>
        <a href="/accounts/login/" class="pull-right">Login</a>
    {% endif %}
</body>
{% endblock content %}
